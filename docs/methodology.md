# Methodology

Port the methods + **Known Limitations** verbatim from the workbench
`docs/METHODOLOGY.md` (in ryanpavlicek/linearaworkbench) as each analysis lands.
Every exploratory method (cross-linguistic distance, morphology clustering,
accounting reconciliation, AI-generated readings) must carry its caveat in the
docstring and be labeled exploratory/unverified at the point of use.

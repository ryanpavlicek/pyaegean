__pycache__/
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.pyc
.coverage
htmlcov/
.ipynb_checkpoints/

# mkdocs build output
site/
_site/
_site_test/

# Local secrets — API keys etc. NEVER committed
.env
.env.*

# training artifacts (datasets rebuild from cache; bake-off checkpoints are throwaway)
training/data/
training/out/
training/results/a17-environment/

# SigLA-derived dataset (CC BY-NC-SA): built by scripts/build_sigla_corpus.py,
# published only as a clearly-labeled release asset - never in the Apache-2.0 tree
sigla-corpus.json

# Large training artifacts (published as release assets, never committed)
training/results/*.zip
training/results/*.tar.gz
training/results/*.onnx
